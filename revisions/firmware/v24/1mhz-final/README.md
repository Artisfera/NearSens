# NearSens v24 1 MHz final

Keep all `.cpp` files and `NearSens.h` in this folder because Arduino compiles them together as one sketch.

Use Arduino IDE with megaTinyCore and ATtiny1626 selected. Set the clock according to the test profile before compiling.
